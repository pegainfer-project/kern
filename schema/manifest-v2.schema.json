{
  "$defs": {
    "Arg": {
      "anyOf": [
        {
          "properties": {
            "buf": {
              "type": "string"
            },
            "offset": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "buf"
          ],
          "type": "object"
        },
        {
          "properties": {
            "offset": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "state": {
              "type": "string"
            }
          },
          "required": [
            "state"
          ],
          "type": "object"
        },
        {
          "properties": {
            "sym": {
              "type": "string"
            }
          },
          "required": [
            "sym"
          ],
          "type": "object"
        },
        {
          "description": "Symbol-derived integer scalar, e.g. `{\"expr\": {\"mul\": [\"tokens\", 32]}}`.\nSame closed expression language as launch grids; f32 params cannot\nbind one.",
          "properties": {
            "expr": {
              "$ref": "#/$defs/Expr"
            }
          },
          "required": [
            "expr"
          ],
          "type": "object"
        },
        {
          "properties": {
            "i32": {
              "format": "int32",
              "type": "integer"
            }
          },
          "required": [
            "i32"
          ],
          "type": "object"
        },
        {
          "properties": {
            "u32": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "u32"
          ],
          "type": "object"
        },
        {
          "properties": {
            "i64": {
              "format": "int64",
              "type": "integer"
            }
          },
          "required": [
            "i64"
          ],
          "type": "object"
        },
        {
          "properties": {
            "f32": {
              "format": "float",
              "type": "number"
            }
          },
          "required": [
            "f32"
          ],
          "type": "object"
        },
        {
          "properties": {
            "u8": {
              "format": "uint8",
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "u8"
          ],
          "type": "object"
        }
      ],
      "description": "A dispatch argument. Explicitly tagged so a buffer name can never be\nmistaken for a symbol name: `{\"buf\": \"hidden\"}`, `{\"state\": \"kv\"}`,\n`{\"sym\": \"tokens\"}`, `{\"i32\": 2560}`, `{\"i64\": 4096}`, `{\"f32\": 1e-6}`.\n\nBuffer and state args carry an optional byte `offset` (default 0): the\nkernel receives base + offset. This is how a provider addresses a view\ninside a fused buffer (q/k/v slices of a merged qkv projection) or a\nper-layer region inside an opaque state — the offset is a literal from\nthe provider's own layout arithmetic, the runtime just adds it."
    },
    "Bound": {
      "anyOf": [
        {
          "format": "int64",
          "type": "integer"
        },
        {
          "format": "double",
          "type": "number"
        },
        {
          "$ref": "#/$defs/Expr"
        }
      ],
      "description": "A domain bound: a literal integer, a literal float, or a symbol\nexpression (evaluated at the caller's symbol values)."
    },
    "Buffer": {
      "additionalProperties": false,
      "properties": {
        "class": {
          "$ref": "#/$defs/BufferClass"
        },
        "domain": {
          "anyOf": [
            {
              "$ref": "#/$defs/Domain"
            },
            {
              "type": "null"
            }
          ],
          "description": "Optional prior on the buffer's *contents*. Never required: a manifest\nwithout domains runs exactly the same. With one, the runtime rejects\nout-of-domain input writes, and attestation can synthesize valid\nvalues for the buffer (and check produced values against it)."
        },
        "dtype": {
          "$ref": "#/$defs/DType"
        },
        "shape": {
          "items": {
            "$ref": "#/$defs/Dim"
          },
          "type": "array"
        }
      },
      "required": [
        "dtype",
        "shape",
        "class"
      ],
      "type": "object"
    },
    "BufferClass": {
      "oneOf": [
        {
          "const": "input",
          "description": "Written by the runtime before program execution.",
          "type": "string"
        },
        {
          "const": "output",
          "description": "Read back by the runtime after program execution.",
          "type": "string"
        },
        {
          "const": "weight",
          "description": "Bound by name from the weight artifact at load time.",
          "type": "string"
        },
        {
          "const": "workspace",
          "description": "Planned and owned by the runtime; contents dead across program runs.",
          "type": "string"
        },
        {
          "const": "carry",
          "description": "Written by one program, read by another; contents persist across\nprogram runs. Which program runs first is the caller's contract —\nthe verifier only requires that *some* program writes it.",
          "type": "string"
        }
      ]
    },
    "DType": {
      "description": "Element type of a buffer or scratch declaration.",
      "enum": [
        "bf16",
        "f16",
        "f32",
        "fp8e4m3",
        "i32",
        "u32",
        "i64",
        "u8"
      ],
      "type": "string"
    },
    "Dim": {
      "anyOf": [
        {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        {
          "type": "string"
        }
      ]
    },
    "Dispatch": {
      "additionalProperties": false,
      "description": "A call site: which kernel interface, with which args. No geometry —\ngrid/block belong to the kernel's implementation.",
      "properties": {
        "args": {
          "items": {
            "$ref": "#/$defs/Arg"
          },
          "type": "array"
        },
        "kernel": {
          "type": "string"
        },
        "label": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "kernel",
        "args"
      ],
      "type": "object"
    },
    "Domain": {
      "additionalProperties": false,
      "description": "A prior on buffer contents, declared by whoever wires the model — the\nonly party that knows a `buffer<i32>` is a page table and not an\nactivation. It is unary (one buffer, no relations between buffers) and\nsays nothing about kernel behaviour: a kernel that misbehaves on\nin-domain input is a kernel bug the harness can now provoke.\n\nTwo forms, mutually exclusive:\n- `min`/`max`: inclusive bounds (like `Symbol`), integers, floats or a\n  symbol expression; either may be omitted. Float buffers without a\n  domain are implicitly \"any finite value\".\n- `index_into`: every element indexes a row of the named buffer or a\n  token slot of the named state, `unit` rows/tokens per index (a paged\n  KV block table indexes 16 tokens at a time).\n\n`monotone` additionally requires a non-decreasing sequence (prefix sums\nsuch as `cu_seqlens`).",
      "properties": {
        "index_into": {
          "type": [
            "string",
            "null"
          ]
        },
        "max": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bound"
            },
            {
              "type": "null"
            }
          ]
        },
        "min": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bound"
            },
            {
              "type": "null"
            }
          ]
        },
        "monotone": {
          "type": "boolean"
        },
        "unit": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "Expr": {
      "anyOf": [
        {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        {
          "properties": {
            "sym": {
              "type": "string"
            }
          },
          "required": [
            "sym"
          ],
          "type": "object"
        },
        {
          "properties": {
            "ceil_div": {
              "maxItems": 2,
              "minItems": 2,
              "prefixItems": [
                {
                  "$ref": "#/$defs/Expr"
                },
                {
                  "format": "uint64",
                  "minimum": 0,
                  "type": "integer"
                }
              ],
              "type": "array"
            }
          },
          "required": [
            "ceil_div"
          ],
          "type": "object"
        },
        {
          "properties": {
            "mul": {
              "maxItems": 2,
              "minItems": 2,
              "prefixItems": [
                {
                  "$ref": "#/$defs/Expr"
                },
                {
                  "format": "uint64",
                  "minimum": 0,
                  "type": "integer"
                }
              ],
              "type": "array"
            }
          },
          "required": [
            "mul"
          ],
          "type": "object"
        }
      ],
      "description": "The closed scalar-expression set. This is deliberately not a language:\ngrid geometry and dynamic shared memory are the only runtime-dependent\nnumbers a provider may compute, and only with these forms."
    },
    "Impl": {
      "additionalProperties": false,
      "description": "An implementation is a micro-program: private scratch buffers (sized by\nexpressions over the interface's symbols, provisioned by the runtime,\ndead outside one dispatch) and one or more launch steps. Launch geometry\nlives here, not at the call site — it belongs to the implementation.",
      "properties": {
        "scratch": {
          "additionalProperties": {
            "$ref": "#/$defs/Scratch"
          },
          "type": "object"
        },
        "steps": {
          "items": {
            "$ref": "#/$defs/Step"
          },
          "type": "array"
        }
      },
      "required": [
        "steps"
      ],
      "type": "object"
    },
    "Kernel": {
      "additionalProperties": false,
      "description": "A kernel is an **interface** (the typed params a dispatch passes — the\nonly thing a call site knows) plus an **implementation**: how those args\nare lowered onto actual launches. Swapping the implementation — a faster\nkernel from elsewhere with the same interface — touches only `impl`;\nevery dispatch stays untouched.",
      "properties": {
        "impl": {
          "$ref": "#/$defs/Impl"
        },
        "params": {
          "description": "The interface: typed, directional params. Call-site semantics\n(what each position means) are the contract implementations must\nhonor; the verifier can only check shape, not meaning.",
          "items": {
            "$ref": "#/$defs/ParamType"
          },
          "type": "array"
        }
      },
      "required": [
        "params",
        "impl"
      ],
      "type": "object"
    },
    "Meta": {
      "additionalProperties": false,
      "properties": {
        "model": {
          "description": "Free-form label; the runtime assigns no meaning to it.",
          "type": "string"
        },
        "spec": {
          "anyOf": [
            {
              "$ref": "#/$defs/SpecMeta"
            },
            {
              "type": "null"
            }
          ],
          "description": "Caller contract of a speculative-decoding manifest (absent for plain\nprefill/decode ones). The runtime assigns no meaning to it."
        },
        "version": {
          "description": "Manifest format version. Only 2 is accepted.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "version",
        "model"
      ],
      "type": "object"
    },
    "ParamType": {
      "description": "One kernel parameter: a by-value scalar (`\"i32\"`, `\"u32\"`, `\"i64\"`, `\"f32\"`, `\"u8\"`) or a directional pointer — `\"in\"`/`\"out\"`/`\"inout\"` followed by `\"ptr\"` (untyped, e.g. opaque state) or `\"buffer<dtype>\"`.",
      "pattern": "^(i32|u32|i64|f32|u8|(in|out|inout) (ptr|buffer<(bf16|f16|f32|fp8e4m3|i32|u32|i64|u8)>))$",
      "type": "string"
    },
    "Program": {
      "additionalProperties": false,
      "properties": {
        "dispatches": {
          "items": {
            "$ref": "#/$defs/Dispatch"
          },
          "type": "array"
        }
      },
      "required": [
        "dispatches"
      ],
      "type": "object"
    },
    "Scratch": {
      "additionalProperties": false,
      "description": "One scratch buffer declaration: like a workspace buffer, but private to\nthe implementation.",
      "properties": {
        "dtype": {
          "$ref": "#/$defs/DType"
        },
        "shape": {
          "items": {
            "$ref": "#/$defs/Dim"
          },
          "type": "array"
        }
      },
      "required": [
        "dtype",
        "shape"
      ],
      "type": "object"
    },
    "SpecMeta": {
      "additionalProperties": false,
      "description": "How a driver stages the `draft` / `verify` programs of a speculative\nmanifest: `draft` runs over `block` rows = the anchor token followed by\n`block - 1` copies of `mask_token`, `verify` over `block` rows = the\nanchor followed by the `block - 1` drafted tokens.",
      "properties": {
        "block": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "mask_token": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "block",
        "mask_token"
      ],
      "type": "object"
    },
    "State": {
      "additionalProperties": false,
      "description": "Opaque persistent state. The runtime knows only how many bytes to\nprovision — per token slot (`bytes_per_token`, scaled by the capacity:\npaged KV) or as one fixed block regardless of capacity (`bytes_fixed`:\nper-sequence recurrent state such as a Mamba/GDN conv + SSM state).\nExactly one of the two is non-zero. The internal layout belongs to the\nprovider's kernels, which receive the base pointer as an untyped `ptr`\nparam.",
      "properties": {
        "align": {
          "default": 256,
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "bytes_fixed": {
          "default": 0,
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "bytes_per_token": {
          "default": 0,
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "Step": {
      "additionalProperties": false,
      "description": "One launch inside an implementation.",
      "properties": {
        "args": {
          "description": "Wiring: where each step param comes from — a forwarded interface\narg, a scratch buffer, or an implementation-private literal.",
          "items": {
            "$ref": "#/$defs/StepArg"
          },
          "type": "array"
        },
        "block": {
          "items": {
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        "cubin": {
          "description": "Cubin file (relative to the kernel artifact dir) this step's symbol\nmust come from. Absent: the runtime searches every loaded module\n(disambiguating by param layout). `extern:` symbols have no cubin.",
          "type": [
            "string",
            "null"
          ]
        },
        "grid": {
          "items": {
            "$ref": "#/$defs/Expr"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        "params": {
          "description": "This step's own launch ABI (what the cubin function actually takes).",
          "items": {
            "$ref": "#/$defs/ParamType"
          },
          "type": "array"
        },
        "pdl": {
          "description": "Programmatic dependent launch: the runtime launches this step with\n`CU_LAUNCH_ATTRIBUTE_PROGRAMMATIC_STREAM_SERIALIZATION`, so it may\nstart while the preceding launch is still draining. Only for kernels\nthat put every read of an upstream product and every global write\nbehind `griddepcontrol.wait` (their own inputs — weights, state —\nmay stream ahead of it).",
          "type": "boolean"
        },
        "sha256": {
          "description": "sha256 of that cubin file, checked by the runtime when both are set.",
          "type": [
            "string",
            "null"
          ]
        },
        "shared_mem": {
          "anyOf": [
            {
              "$ref": "#/$defs/Expr"
            },
            {
              "type": "null"
            }
          ],
          "description": "Dynamic shared memory in bytes, if the step needs any."
        },
        "symbol": {
          "description": "Entry symbol, or `extern:<op>` for runtime built-ins.",
          "type": "string"
        }
      },
      "required": [
        "symbol",
        "params",
        "block",
        "grid",
        "args"
      ],
      "type": "object"
    },
    "StepArg": {
      "anyOf": [
        {
          "properties": {
            "arg": {
              "format": "uint",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "arg"
          ],
          "type": "object"
        },
        {
          "properties": {
            "offset": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "scratch": {
              "type": "string"
            }
          },
          "required": [
            "scratch"
          ],
          "type": "object"
        },
        {
          "properties": {
            "i32": {
              "format": "int32",
              "type": "integer"
            }
          },
          "required": [
            "i32"
          ],
          "type": "object"
        },
        {
          "properties": {
            "u32": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "u32"
          ],
          "type": "object"
        },
        {
          "properties": {
            "i64": {
              "format": "int64",
              "type": "integer"
            }
          },
          "required": [
            "i64"
          ],
          "type": "object"
        },
        {
          "properties": {
            "f32": {
              "format": "float",
              "type": "number"
            }
          },
          "required": [
            "f32"
          ],
          "type": "object"
        },
        {
          "properties": {
            "u8": {
              "format": "uint8",
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "u8"
          ],
          "type": "object"
        }
      ],
      "description": "A step argument. `{\"arg\": 0}` forwards the dispatch's arg #0 verbatim;\n`{\"scratch\": \"pmax\"}` passes a private scratch pointer (optional byte\n`offset`); scalar literals are implementation constants the interface\nnever sees (e.g. a partial-count baked into a two-stage reduction)."
    },
    "Symbol": {
      "additionalProperties": false,
      "description": "A runtime-provided scalar (e.g. token count this step). The declared\nbounds are what verification is performed against; the runtime rejects\nout-of-bounds values at dispatch time.",
      "properties": {
        "max": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "min": {
          "default": 1,
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "max"
      ],
      "type": "object"
    }
  },
  "$id": "https://kern-baa.pages.dev/schema/manifest-v2.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "buffers": {
      "additionalProperties": {
        "$ref": "#/$defs/Buffer"
      },
      "type": "object"
    },
    "kernels": {
      "additionalProperties": {
        "$ref": "#/$defs/Kernel"
      },
      "type": "object"
    },
    "meta": {
      "$ref": "#/$defs/Meta"
    },
    "programs": {
      "additionalProperties": {
        "$ref": "#/$defs/Program"
      },
      "type": "object"
    },
    "states": {
      "additionalProperties": {
        "$ref": "#/$defs/State"
      },
      "default": {},
      "type": "object"
    },
    "symbols": {
      "additionalProperties": {
        "$ref": "#/$defs/Symbol"
      },
      "default": {},
      "type": "object"
    }
  },
  "required": [
    "meta",
    "buffers",
    "kernels",
    "programs"
  ],
  "title": "kern manifest v2",
  "type": "object"
}
