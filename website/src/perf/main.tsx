import React from "react";
import ReactDOM from "react-dom/client";
import { PerfPage } from "./PerfPage";
import "./perf.css";

ReactDOM.createRoot(document.getElementById("root")!).render(<React.StrictMode><PerfPage /></React.StrictMode>);
